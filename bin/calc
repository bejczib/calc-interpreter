#!/usr/bin/ruby

require_relative '../lib/interpreter'
require_relative '../lib/lexer'
require_relative '../lib/utils'

while true
  text = Utils.prompt "?> "
  break if text == "exit"
  puts Interpreter.new(Lexer.new(text)).evaluate
end
